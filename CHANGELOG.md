# What's Changed

<!--
Keep only the changes for the current release in this file.
Replace the content when preparing the next release; release history is preserved by GitHub Releases.
-->

- Change `PrivilegeUiConfig.startupModes` to an ordered list so applications can control
  authorization tab order, reject duplicate modes, and show the External tab only when
  external start providers are configured.

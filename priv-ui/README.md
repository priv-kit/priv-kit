# priv-ui

Optional Jetpack Compose UI helper module for Priv Kit.

Namespace and package root: `priv.kit.ui`.

This skeleton module declares the Compose UI boundary only. It does not implement composables, lifecycle controls, system operation UI, or helper behavior.
